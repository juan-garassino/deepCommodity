[pytest]
# Only collect the project's own tests — not the bootcamp notebook solutions
# (notebooks/**/tests/*.py need nbresult + run heavy Keras training). Keeps both
# CI and local `pytest` fast and clean; `make dc-test` already scopes to tests/.
testpaths = tests
